json.extract! business, :id, :name, :img_url, :address, :cross_streets,
                        :neighborhood, :lat, :long, :phone,
                        :site_url, :menu_url,
                        :category, :rating, :cost, :health_score
